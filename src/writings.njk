---
layout: base.njk
title: Writings
permalink: /writings/
---
<section class="writings">
  <h1 class="page-title">Writings</h1>

  {% set lastYear = 0 %}
  <ul class="post-list">
    {% for post in collections.posts %}
      {% set postYear = post.date | year %}
      {% if postYear != lastYear %}
        {% set lastYear = postYear %}
        <li class="post-year" aria-hidden="true">{{ postYear }}</li>
      {% endif %}
      <li class="post-item">
        <time class="post-date" datetime="{{ post.date | isoDate }}">{{ post.date | shortDate }}</time>
        <a class="post-link" href="{{ post.url }}">{{ post.data.title }}</a>
      </li>
    {% endfor %}
  </ul>

  {% if collections.posts.length == 0 %}
  <p class="empty">Nothing here yet.</p>
  {% endif %}
</section>
